# TODO

- [ ] Fix PHP parse/typing issues reported by Intelephense in `app/Http/Controllers/EventController.php` and `app/Http/Controllers/CategoryController.php`.
- [ ] Run `php -l` (lint) on the modified controllers.
- [ ] Run a quick `php artisan route:list` (if needed) / `composer test`-style checks (if available).

